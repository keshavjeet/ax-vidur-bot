{
  "name": "node-downstream",
  "version": "0.1.0",
  "private": true,
  "description": "Node workspace: app and packages",
  "scripts": {
    "build": "tsc -b",
    "start": "npm start -w @node-downstream/app",
    "test:client": "npm run test:client -w @node-downstream/app",
    "test:websocket": "npm run test -w @node-downstream/websocket",
    "test:audio": "npm run test -w @node-downstream/audio"
  },
  "workspaces": [
    "app",
    "packages/websocket",
    "packages/audio"
  ],
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
